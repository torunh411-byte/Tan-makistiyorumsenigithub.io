<!-- index.html -->
<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tanışma Sitesi</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #ffe6f2, #fff0f6);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      animation: bgShift 10s infinite alternate;
    }

    @keyframes bgShift {
      0% { background-position: 0% 50%; }
      100% { background-position: 100% 50%; }
    }

    .card {
      background: white;
      width: 90%;
      max-width: 400px;
      padding: 30px 25px;
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
      text-align: center;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.25);
    }

    h2 {
      margin-bottom: 25px;
      color: #d63384;
      font-size: 1.8rem;
      letter-spacing: 1px;
    }

    input, textarea {
      width: 100%;
      padding: 14px;
      margin: 12px 0;
      border-radius: 15px;
      border: 1px solid #f5c2e7;
      font-size: 15px;
      transition: 0.3s;
    }

    input:focus, textarea:focus {
      border-color: #ff4da6;
      box-shadow: 0 0 10px rgba(255,77,166,0.4);
      outline: none;
    }

    textarea {
      min-height: 120px;
      resize: vertical;
      background: linear-gradient(135deg, #ffe6f2, #fff0f6);
      box-shadow: inset 0 3px 8px rgba(0,0,0,0.05);
    }

    textarea::placeholder, input::placeholder {
      color: #cc6699;
      font-style: italic;
    }

    button {
      padding: 14px;
      width: 100%;
      background: linear-gradient(45deg, #ff4da6, #ff80bf);
      color: white;
      font-size: 16px;
      font-weight: bold;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: 0.3s;
      box-shadow: 0 5px 15px rgba(255,77,166,0.4);
    }

    button:hover {
      background: linear-gradient(45deg, #e60073, #ff4da6);
      box-shadow: 0 8px 20px rgba(230,0,115,0.5);
    }

    @media (max-width: 480px) {
      h2 {
        font-size: 1.5rem;
      }
      input, textarea, button {
        font-size: 14px;
      }
      .card {
        padding: 25px 15px;
      }
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>💖 Profil Oluştur</h2>
    <form action="https://formspree.io/f/movnwelr" method="POST">
      <input type="text" name="kullaniciAdi" placeholder="İsmini çok merak ediyorum 💕" required>
      <textarea name="hakkinda" placeholder="❤️ Kendini tatlı bir şekilde anlat..." required></textarea>
      <input type="url" name="fotoURL" placeholder="📸 Fotoğraf linkini buraya yapıştır..." required>
      <input type="hidden" name="_replyto" value="torunh411@gmail.com">
      <button type="submit">💌 Gönder</button>
    </form>
  </div>
</body>
</html>